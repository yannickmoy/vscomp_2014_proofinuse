project Simple is

   for Main use ("simple");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnata", "-gnato", "-gnatVa", "-fstack-check");
   end Compiler;

   package Prove is
      for Switches use ("-j0");
   end Prove;

end Simple;
